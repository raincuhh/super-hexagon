export const canvasW = 900;
export const canvasH = 700;
export const canvasCenter = {
	x: canvasW / 2,
	y: canvasH / 2,
};

export const DIST_FROM_CIRCLE = 92;
export const PATTERN_SPAWN_RADIUS = Math.min(canvasW, canvasH) / 2 + 300;
export const WALL_SPEED = 300;
